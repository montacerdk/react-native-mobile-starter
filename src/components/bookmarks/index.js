import { Text, View } from 'react-native';
import React from 'react';

import styles from './styles';

const Bookmarks = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.content}>Bookmarks Screen ...</Text>
    </View>
  );
};

export default Bookmarks;
